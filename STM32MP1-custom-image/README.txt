This folder contains the bootfs image as well as the rootfs image. The kernel is configured to enable I2C. 
The application that reads data from AXML345 accelerometer is baked into the kernel under the name "getaccl".
This executeable file can be found in the /usr/bin directory of the rootfs.
